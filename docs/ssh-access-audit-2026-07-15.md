# meetandgreet.jp 生产主机 SSH 接入审计

**日期：** 2026-07-15（GMT+9）  
**执行角色：** ②全栈工程师AI（生产部署）  
**授权人：** 中村先生

## 审计目标

本次仅验证生产主机 SSH 服务可达性、主机身份与授权账户认证，不修改远端文件、数据库、服务、计划任务或配置。明文密码和私钥不写入 Git、事实源、日志或交付文档。

## 目标与安全门禁

| 项目 | 结果 |
|---|---|
| 目标地址 | `195.35.62.119:65002` |
| SSH 用户 | `u907480505` |
| 主机身份校验 | 使用独立 `known_hosts` 文件并启用严格校验 |
| 转发能力 | Agent、X11、本地与远程转发均禁用 |
| 认证次数 | 按批准计划仅尝试一次密码认证 |
| 远端写入 | 无 |

## 主机密钥基线

| 算法 | SHA-256 指纹 |
|---|---|
| RSA | `SHA256:C0RGre3JyRrwV03xDLcyNceJxiPgp4R9bq51Ql22NQ4` |
| ECDSA | `SHA256:CwPA3JoPD2BAfYwRIcVJMTYmtseF+1QojW+8aKriDYo` |
| ED25519 | `SHA256:oEkp92RGLqbPKOhghCAf5SVr0VQwJs96Pl6ohCnQp+Y` |

服务端识别为 OpenSSH 9.9。后续连接必须继续使用严格主机密钥校验；若指纹变化，应立即停止并通过 Hostinger 控制面板核实，不得直接接受新指纹。

## 认证结果

单次认证返回：

```text
Permission denied (publickey,password).
```

因此，本次已确认 SSH 端口和服务可达，但现有密码不能完成 `u907480505` 的 SSH 认证。按照批准计划，认证失败后未重复尝试，未进入远端 Shell，也未开展 WordPress 路径、PHP、WP-CLI、数据库或权限审计。

## 零写入结论

本次没有登录远端主机，没有执行任何远端命令，没有修改文件、数据库、服务、缓存、计划任务、SSH 配置或防火墙。生产 WordPress 与此前已完成的 0.2.5 部署状态不受影响。

## 继续执行所需最小条件

可采用下列任一方式恢复后续只读审计：

| 方式 | 所需资料 | 安全要求 |
|---|---|---|
| SSH 专用密码 | 确认 `u907480505` 的主机 SSH 密码 | 仅一次认证；不得写入仓库或报告 |
| SSH 私钥 | 与服务器已登记公钥匹配的私钥文件 | 以附件提供；权限设为 `0600`；验证公钥指纹后使用；完成后清理 |
| Hostinger 控制面板重置 | 在 hPanel 中重置 SSH 密码或重新登记公钥 | 先核对用户名、端口和公钥，再重试一次 |

当前仅提供了公钥文本，客户端登录仍需要与之匹配的私钥；公钥本身不能代替私钥完成认证。

## 后续只读审计范围

认证打通后，第一轮只允许执行身份、系统版本、磁盘、PHP/WP-CLI、WordPress 根目录定位、Git 状态、备份目录与权限边界检查。任何文件修改、数据库导出、部署、重启或权限调整均须在只读审计完成后另设检查点。
