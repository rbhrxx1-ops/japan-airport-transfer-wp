# meetandgreet.jp 生产主机 SSH 接入审计

**日期：** 2026-07-15（GMT+9）
**执行角色：** ②全栈工程师AI（生产部署）
**授权人：** 中村先生
**最终结论：** SSH 专用公钥认证成功，只读访问通道已建立

## 审计目标

本次用于验证生产主机 SSH 服务、主机身份、新 ED25519 公钥认证及只读运维能力。审计不修改远端文件、数据库、WordPress 设置、服务、计划任务、缓存或 Git 工作树；明文密码和私钥不写入 Git、事实源、日志或交付文档。

## 目标与安全门禁

| 项目 | 结果 |
|---|---|
| 目标地址 | `195.35.62.119:65002` |
| SSH 用户 | `u907480505` |
| 远端主机名 | `sg-nme-web483.main-hosting.eu` |
| 客户端公钥指纹 | `SHA256:ODo/BsBsyirSKI6dLTjXRXxfCKRaV/sU659N2iBPzvA` |
| 私钥权限 | `0600`，位于项目仓库外 |
| 主机身份校验 | 三类指纹全部匹配既有基线；严格校验启用 |
| 认证方式 | 非交互式公钥认证；禁用密码和键盘交互认证 |
| 转发能力 | Agent、X11、本地与远程转发全部禁用 |
| 交互式终端 | 未开启 PTY |
| 远端写入 | 无 |

## 主机密钥基线复核

| 算法 | SHA-256 指纹 | 结果 |
|---|---|---|
| RSA | `SHA256:C0RGre3JyRrwV03xDLcyNceJxiPgp4R9bq51Ql22NQ4` | 匹配 |
| ECDSA | `SHA256:CwPA3JoPD2BAfYwRIcVJMTYmtseF+1QojW+8aKriDYo` | 匹配 |
| ED25519 | `SHA256:oEkp92RGLqbPKOhghCAf5SVr0VQwJs96Pl6ohCnQp+Y` | 匹配 |

连接继续使用隔离的临时 `known_hosts` 文件和严格主机密钥校验。若未来任何指纹变化，应立即停止并通过 Hostinger 控制面板核实，不得绕过校验。

## 认证结果

新 ED25519 公钥保存至 Hostinger 后，非交互式认证成功。远端返回的身份信息如下：

| 项目 | 结果 |
|---|---|
| 用户 | `u907480505` |
| UID | `907480505` |
| 主目录 | `/home/u907480505` |
| 主机名 | `sg-nme-web483.main-hosting.eu` |
| 账户类型 | Hostinger 受限共享主机账户 |

未使用密码，没有重复失败认证，没有使用 `sudo`，也没有修改 `authorized_keys` 或服务器 SSH 配置。

## 只读环境审计

| 检查项 | 结果 |
|---|---|
| 内核 | Linux `5.14.0-611.16.1.el9_7.x86_64` |
| 主目录文件系统 | 7.0 TB，总使用率 80%，可用约 1.5 TB |
| PHP CLI | PHP `8.3.30` |
| MariaDB 客户端 | MariaDB `11.8.8` |
| WP-CLI | `2.12.0`，可正常读取站点 |
| WordPress 根目录 | `/home/u907480505/domains/meetandgreet.jp/public_html` |
| WordPress 核心 | `7.0.1` |
| `home` / `siteurl` | 均为 `https://meetandgreet.jp` |
| 活动主题 | `jat-meet-theme` `0.2.5` |
| 预约插件 | `jat-reservation` `0.2.4`，活动 |
| 缓存插件 | LiteSpeed Cache `7.8.1`，活动 |
| 备份插件 | UpdraftPlus `1.26.5`，活动 |
| 备份目录 | `wp-content/updraft`，10 个文件，约 72 MB |
| 生产 Git | WordPress 根目录无 `.git`，生产不是直接 Git 工作树 |

WordPress 根目录及 `wp-content`、主题、插件、上传、UpdraftPlus 备份目录均由 `u907480505` 所有，目录权限为 `0755`。审计仅读取版本、状态、路径和文件元数据，没有读取 `wp-config.php` 正文、数据库密码、真实订单、备份正文或个人数据。

插件状态核验发现 `hostinger-reach` `1.5.8` 有可用更新；本轮未执行更新，因为这属于生产写入，必须另设检查点。其余已列插件在审计时未报告可用更新。

## 零写入结论

本次远端操作全部为身份、版本、状态和元数据查询。未修改文件、数据库、WordPress 设置、插件、主题、缓存、服务、计划任务、权限、SSH 配置或防火墙，未执行部署或重启。生产主题保持 `0.2.5`，生产站点 URL 保持 `https://meetandgreet.jp`。

## 运维通道结论与后续边界

**SSH 只读访问通道已建立。** 后续可使用该通道进行高效审计、版本确认、日志定位和部署前检查，但默认仍为只读。任何文件修改、数据库导出或变更、插件更新、主题部署、缓存清理、服务重启或权限调整，必须先明确变更范围、备份与回滚点，并建立独立执行检查点。

私钥仅保存在受控环境中且位于项目仓库外。不得将私钥复制到仓库、项目文档、任务书、聊天正文或其他共享位置。
