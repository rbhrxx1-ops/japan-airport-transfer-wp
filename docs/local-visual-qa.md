# 本地视觉验收记录

## 2026-07-11 首页首轮检查

本地地址：`http://127.0.0.1:8080/`。专属主题已正常激活，首页以日语完整渲染，页头、四项服务、选择理由、六步流程、欢迎牌示例、地点、法人服务、FAQ、页尾转化和法务导航均存在。主按钮与层级清晰，桌面端服务卡片保持四列，正文可读。

已发现问题：深色首屏中的眉题“空港・駅 ミート＆センディング”对比度过低，几乎不可见；需修正为浅色或金色。首页正文未发现中文残留。公司资料、联系方式与法务内容仍按批准方案保留为待责任人确认的草稿或提示，不得擅自发布虚构信息。

预约入口目前仅链接到占位页面，符合阶段三边界；实际五步预约将在阶段四实现。
## 2026-07-11 首页修复复验与服务详情页

首屏眉题已改为金色，在深海军蓝背景上清晰可读；页头、主标题、按钮与服务区未出现回归。

代表页 `service/airport-meet/` 正常渲染，标题、摘要、适用场景、标准内容、五步流程、申请所需信息、变更与未会合处理、预约/咨询转化均完整，未发现中文残留或超出已确认业务边界的承诺。桌面端卡片与正文可读。

待改进：通用固定页面模板的面包屑当前统一显示“ホーム ／ ご案内”，服务子页应显示“ホーム ／ サービス ／ 空港お迎え”等真实层级；阶段三内修正为根据父级与当前页动态生成的面包屑。
## 2026-07-11 动态面包屑复验

通用固定页面模板已改为动态输出真实层级。`/service/airport-meet/` 浏览器复验显示“ホーム ／ サービス ／ 空港お迎え”，父级链接、当前页文本和 `aria-current="page"` 均正确；标题区布局正常。命令行抓取亦确认生成了带 `aria-label="パンくずリスト"` 的 `<nav>`。

## 2026-07-11 结构化 FAQ 验收

本地 `/faq/` 已由 `jat_faq` 结构化内容与 `[jat_faq_list]` 动态输出。浏览器确认五个分类、二十四条折叠问答、动态面包屑、页头页尾与转化入口均正常；问答内容默认折叠，键盘可聚焦原生 `summary`，未发现脚本注入或布局溢出。

发现一处日语用词需要修正：问题“外語での対応はできますか。”应改为更自然的“外国語での対応はできますか。”。该问题将同时修正初始化数据并通过幂等脚本更新现有本地条目。

## 2026-07-11 阶段四：五步在线预约

验收地址为 `http://127.0.0.1:8080/reservation/?qa=phase4`，仅在本地隔离环境运行。预约插件已在 WordPress 7.0.1 激活；订单表 `wp_jat_orders`、短代码 `[jat_reservation_form]` 与 REST 路由 `/jat-reservation/v1/orders` 均已注册。

页面实际渲染五步进度、日语说明、错误摘要、草稿保存提示，以及“发送时仅为受理、并非预约确定”的业务边界文案。标准“空港お迎え”分支实测时，选择羽田机场后显示航班号并隐藏列车字段；第一步填写日期、时间、航班号及行程后可进入第二步。第二步的代表者、人数、行李、受托行李、紧急联系人与移动配虑均可填写，`support_other` 已显示为“その他の配慮を相談”，通过校验后可进入第三步。

第三步迎接分支显示欢迎牌名称、公司名、语言与实时预览，且明确标注“プレビューはイメージです”。蜜罐字段设有 `aria-hidden="true"`、`tabindex="-1"` 并通过绝对定位移出可视区域；浏览器文本抽取仍会列出该字段，但实际截图中未占用可见表单布局。

仍需完成第三至第五步整流程提交、服务器端字段错误映射、幂等重复提交、限流、时间陷阱、咨询/送行/车站/法人条件分支、草稿恢复及移动端浏览器验收。

完整标准分支已从第一步执行到第五步并成功提交至本地 REST 接口。确认页正确汇总服务、地点、航班、人数、行李、配虑、欢迎牌、申请人、联系方式、目的地与交通方式；三个必需同意项未被自动勾选，用户勾选后才允许提交。成功界面显示测试受付编号 `JAT-202607-GMXES9`、状态“受付済み”，并再次明确“この時点では予約確定ではありません”。该编号与数据只存在于本地隔离测试数据库，不是生产订单。

## 阶段四：预约接口与安全门禁复验

**测试日期：** 2026-07-11（GMT+9）

服务器端集成测试 `tests/phase4-integration.php` 共 **23 项通过、0 项失败**，覆盖六个服务类型、机场/车站地点约束、隐藏字段清理、HTML 清洗、字段长度、邮箱、不可顺序推测受付编号、同一幂等键重试和十五分钟指纹重复检测。

REST 安全与恢复测试 `tests/phase4-api.py` 共 **11 项通过、0 项失败**，覆盖跨站 Origin、同源 Nonce、WordPress 核心过期 Nonce 错误兼容、自动刷新后重试、蜜罐、时间陷阱、结构化字段错误、有效订单写入、幂等重试和十分钟速率限制。测试发现 WordPress 核心会在路由回调前返回 `rest_cookie_invalid_nonce`；前端已兼容该错误码并保留本地草稿后自动刷新 Nonce，避免向日语用户暴露核心英文错误。

阶段四统一门禁再次执行 PHP lint、JavaScript 语法检查、插件激活、23 项集成测试与 11 项 REST 测试，结果为 `PHASE4_GATE=PASS`。测试订单与限流瞬态数据已从隔离环境清理，生产站仍无代码或设置改动。
