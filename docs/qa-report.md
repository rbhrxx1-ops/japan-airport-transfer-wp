# Japan Airport Transfer 阶段六综合 QA 报告

**执行日期：** 2026-07-11

**执行角色：** ②全栈工程师AI（生产部署）

**测试对象：** 本地隔离 WordPress 7.0.1、PHP 8.3、MariaDB；专属主题 `jat-meet-theme`；预约插件 `jat-reservation`

**结论：** 代码质量与本地业务流程门禁通过；生产发布仍被业务资料、完整备份、正式邮件和上线授权阻断。

## 一、综合结果

| 验收组 | 结果 | 证据 |
|---|---:|---|
| PHP / JavaScript 语法 | 通过 | 全部版本化 PHP 文件 `php -l`；全部版本化 JavaScript 文件 `node --check` |
| 六个服务分支与隐藏字段清理 | 23/23 通过 | `tests/phase4-integration.php` |
| REST 安全、Nonce 恢复、幂等与限流 | 11/11 通过 | `tests/phase4-api.py` |
| 订单后台、状态机、权限、邮件审计、隐私 | 31/31 通过 | `tests/phase5-integration.php` |
| 页面树与业务发布边界 | 138/138 通过 | `tests/phase6-business-acceptance.php` |
| 全站日语、SEO、链接、H1、图片与表单无障碍 | 通过 | `tests/phase6-site-quality.py`；抓取 22 个公开页面 |
| 浏览器五步预约 | 通过 | 标准机场迎接流程可完成并生成单一受付编号；记录见 `docs/local-visual-qa.md` |
| 后台最小权限与状态操作 | 通过 | 客服列表脱敏、详情、状态、备注可用；客服无 CSV 权限；记录见 `docs/local-visual-qa.md` |
| Lighthouse | 通过 | 最终报告位于 `reports/lighthouse-final/` |
| 生产发布 | **阻断** | 未取得完整文件+数据库备份、业务确认、正式 SMTP 和上线授权 |

## 二、Lighthouse 最终分数

最终报告在隔离站临时模拟生产可索引状态生成，完成后已自动恢复 `blog_public=0`。本地测试站继续禁止搜索引擎索引。

| 页面 | Performance | Accessibility | Best Practices | SEO |
|---|---:|---:|---:|---:|
| 首页 | 98 | 100 | 100 | 100 |
| 空港お迎え服务页 | 99 | 100 | 100 | 100 |
| FAQ | 99 | 100 | 100 | 100 |
| 在线预约 | 98 | 100 | 100 | 100 |

本轮修复了白色卡片编号的低对比度问题，并在 WordPress 未配置正式站点图标时提供 SVG favicon 回退，消除了浏览器控制台中的 favicon 404。生产配置正式站点图标后，WordPress 原生图标优先，不会重复输出。

## 三、业务意图验收

四类标准服务均包含服务场景、标准服务内容、流程、申请资料、异常处理和“在线提交不等于预约确定”的边界说明。两个咨询分支仅作为个别咨询，不被误标为标准服务。价格页未写入未经确认的金额，仅保留个别报价框架。

地点页已建立羽田、成田、东京站和品川站的可维护页面与核验字段，但未伪造核验日期、负责人或发布就绪状态。公司、联系、隐私、条款、取消政策、特商法与案例框架保持草稿，未公开未确认资料。

预约流程覆盖条件字段切换、隐藏字段清理、草稿恢复、日语字段错误、欢迎牌预览、摘要确认、同意项、幂等提交和受付编号。订单后台覆盖最小权限、脱敏列表、完整详情、确定性状态机、内部备注、状态历史、邮件日志、审计日志、受控 CSV 导出及 WordPress 原生个人数据导出/匿名化。

## 四、安全与隐私验收

接口已验证同源限制、REST Nonce、过期 Nonce 自动刷新、蜜罐、时间陷阱、十分钟窗口限流、服务器端字段白名单、条件必填、HTML 清洗、不可顺序推测受付编号、幂等键及短窗指纹去重。测试未接触生产客户数据。

邮件日志仅保存收件人哈希，不保存收件地址；原始提交快照与当前快照分离；个人数据删除使用匿名化并保留必要审计事实。实际保留期限仍需业务和法务确认，未在代码中擅自设定自动删除期限。

## 五、当前生产站只读观察

2026-07-11 对 `https://japanairporttransfer.com/` 的只读响应头检查显示 HTTPS 为 HTTP/2 200，服务器当前公开 `x-powered-by: PHP/8.3.30`、Hostinger 平台标识，并仅见 `content-security-policy: upgrade-insecure-requests`。这些是现有生产环境事实，不代表本项目已部署。生产发布前应在主机/CDN层评估移除版本暴露，并补齐适合 WordPress 的 HSTS、`X-Content-Type-Options`、`Referrer-Policy`、`Permissions-Policy` 与点击劫持防护；CSP 应先以报告模式验证，避免破坏后台和插件。

## 六、明确发布阻断项

| 阻断项 | 当前状态 | 放行条件 |
|---|---|---|
| 完整生产备份 | 仅有 WordPress WXR 内容导出，不含主题、插件、上传、配置和数据库完整快照 | 取得主机文件与数据库完整备份，并验证可恢复 |
| 公司资料 | 未确认 | 提供正式日文会社名、地址、代表、电话、营业时间、资质与业绩 |
| 法务与保留期限 | 未确认 | 审核隐私、条款、取消政策、特商法和数据保留期限 |
| 价格与取消规则 | 未确认 | 确认基础费、追加费、税、支付、取消和变更规则 |
| 地点现场规则 | 未核验 | 为四个地点填写最终核验日期、责任人、集合点和车寄规则 |
| 正式邮件 | 未配置 | 确认发件名、客服邮箱、告警收件人并配置可靠 SMTP/事务邮件 |
| 品牌素材 | 未提供 | 提供 Logo、品牌规范与自有/授权图片，或书面接受当前纯图形占位方案 |
| 生产上线授权 | 未取得 | 中村先生明确转达 PM-AI 验收结论与生产上线授权 |

## 七、结论

当前代码可进入 PM-AI 的阶段六验收，但**不能写成“项目完成”或直接部署生产**。下一阶段仅在完整备份、业务资料、正式邮件方案与上线授权全部满足后执行生产发布、四类服务回归、Git/生产一致性核对和最终交付。

## 八、2026-07-15 域名变更回归

主域名变更为 `meetandgreet.jp` 后，重新执行 PHP/JavaScript/Shell 语法、阶段四集成、REST 安全、阶段五集成、阶段六业务边界、全站质量、差异完整性、凭据扫描及运行时旧域硬编码检查。阶段四为 **23/23**，REST 为 **11/11**，阶段五为 **31/31**，业务边界为 **138/138**，全站质量抓取 **22** 个公开页面并通过；六个业务/法务页面按既有门禁保持草稿。

独立只读域名门禁 `tests/domain-migration-readonly.sh` 为 **5 项通过、2 项失败**。新根域 HTTPS、canonical、WordPress `url/home`、根域证书及运行时代码扫描通过；`www.meetandgreet.jp` 证书未覆盖 `www`，旧域名 `japanairporttransfer.com` 当前不可达，无法验证按原路径 301。域名迁移因此处于“新根域已生效、SEO 迁移未闭环”状态，不能标记为全部完成。
