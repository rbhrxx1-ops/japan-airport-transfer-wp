# 任务书：MG-CONTENT-20260715 竞品调查报告内容整改

**执行角色：** ②全栈工程师AI（生产部署）  
**汇报对象：** 中村先生  
**仓库：** `github.com/rbhrxx1-ops/japan-airport-transfer-wp`  
**分支：** `main`  
**治理基线：** `06c555ca197cdd9f5a26b5d21fc7a87e96b40ebf`  
**生产地址：** `https://meetandgreet.jp`  
**生产根目录：** `/home/u907480505/domains/meetandgreet.jp/public_html`  
**任务输入：** 《机场_车站迎接服务（MeetandGreet）竞品与现状分析整改报告》及 `docs/content-remediation-matrix-2026-07-15.md`

## 一、任务目标

在不虚构金额、取消比例、电话号码、即时通讯账号、回复时效、车型容量、语言承诺、客户评价、客户 Logo、服务件数或未授权素材的前提下，强化网站对“迎送人员、预约车辆/司机、委托方”三方协同价值的表达，提升首页、服务、流程、价格、区域和法人页面的可理解性与询价转化路径。

> 外部报告是问题线索，不是可直接发布的事实来源。生产只读核验已证明：特商法页正文未重复，电话号码并未缺失；因此不得按误报删除或替换现有法务与公司信息。

## 二、允许与禁止范围

| 范围 | 规定 |
|---|---|
| 允许修改 | `bin/seed-site.php`、内容整改专用测试、`truth.md`、`plan.md`、本任务书、整改矩阵、验收报告、`.pm-ai/changelogs/` |
| 条件允许 | 若内容需要小型展示样式，可最小修改 `wp-content/themes/jat-meet-theme/style.css` 并递增主题版本；无必要不得改 CSS |
| 禁止修改 | `wp-content/plugins/jat-reservation` 的订单、状态机、数据库、权限和提交 API；WordPress 核心；现有电话、公司法定资料、法律条款事实；未在任务书内的功能 |
| 禁止发布 | 未确认价格、取消数字、WhatsApp/LINE、固定 SLA、车型容量、无条件多语言承诺、虚构案例/评价/Logo/统计数字、未授权图片或地图 |
| 生产写入门禁 | 本地测试全部通过；创建并校验新的完整 UpdraftPlus 备份；建立文件与数据库回滚点；严格校验 SSH 主机指纹；仅部署已测试的最小差异 |

## 三、实施步骤

1. 执行 `git fetch origin && git reset --hard origin/main`，确认 HEAD 为任务书治理基线；本轮已生成的治理文档须先纳入任务书提交后再进入代码修改。
2. 修改 `bin/seed-site.php`，仅重写现有页面的数据源，不重复创建同义页面：
   - 首页：在主视觉和价值区明确“会合人员—车辆/司机—委托方”的协同目标，保持申请送信不等于预约确认。
   - 服务页：明确可咨询的标准支援范围、需事前确认的边界与车辆协同方式。
   - 流程页：补充会合、车辆引导、异常联络及完成报告的可验证流程；不承诺实时照片报告或固定响应时限。
   - 价格页：增加报价构成类别、费用可能变化因素和书面确认节点；不填写金额或取消比例。
   - 区域页：区分羽田、成田、东京、品川核心展示地点与其他日本/亚洲机场的个别咨询范围；不承诺全区域无条件覆盖。
   - 法人页：加入非真实订单的“完了报告项目示例”，注明具体报告项目依个别约定。
   - 联系/预约入口：保持现有正式邮箱和电话，不新增 WhatsApp、LINE 或 SLA。
3. 不修改特商法、公司法定资料、电话、利用条款和取消政策；以自动化断言保护其不被误删，并验证 `/legal/` 主正文仅单次渲染。
4. 新增或更新内容整改测试，至少验证：三方协同核心文案、价格无金额占位、报告示例免责声明、核心/其他地点边界、申请不等于预约确认、法务电话存在、禁止词与虚构承诺缺失。
5. 运行 PHP lint、内容整改测试、现有阶段四/五/六测试、全站质量门禁；所有命令必须 `exit 0`。检查公开页面无中文、无开发占位符、无未授权品牌或竞品复制。
6. 在生产写入前通过 UpdraftPlus 创建新的完整备份，记录时间、组成、文件数量和可恢复入口；通过 SSH 记录目标文件与相关页面的部署前哈希/状态。
7. 仅同步已测试的主题/内容种子差异；以幂等方式运行种子更新现有页面。不得修改真实订单、用户、预约数据库、插件配置或法务资料。
8. 清理 LiteSpeed 页面缓存后执行生产验收：桌面和 390px 移动端首页、服务、流程、价格、区域、法人、FAQ、预约、公司、联系、特商法；检查 HTTP、H1、canonical、JSON-LD、导航、CTA、横向溢出和浏览器控制台。
9. 若任一关键门禁失败，立即停止，恢复部署前文件与数据库回滚点，清理缓存并重新验收；不得把失败记录为完成。
10. 更新 `truth.md`、`plan.md`、生产验收报告和 `.pm-ai/changelogs/`，执行敏感信息扫描，提交并推送 `origin/main`。

## 四、检查点与完成标准

| 检查点 | 完成标准 |
|---|---|
| CP1 事实裁决 | 外部报告每项建议已标记为立即实施、部分实施、已具备/核验或阻断；误报不进入代码 |
| CP2 本地实现 | 仅修改任务书允许文件；内容为原创自然日语；无未经确认事实 |
| CP3 自动化门禁 | 新增测试和现有全量门禁全部通过，失败数为 0 |
| CP4 生产回滚 | 新完整备份存在且恢复入口可用；部署前后哈希和页面状态已记录 |
| CP5 生产验收 | 所有目标页面 HTTP 200；核心新文案可见；法务正文单次渲染；电话与公司资料未被破坏；无控制台错误或移动端横向溢出 |
| CP6 Git 闭环 | 工作树干净，提交已推送，报告含完整 commit hash、测试结果、备份和已知阻断项 |

## 五、闭环动作

完成后向中村先生回报：完整 commit hash、变更页面、测试命令与结果、生产备份时间、部署与回滚证据、生产桌面/移动验收结果，以及仍受业务资料阻断的第二批整改项。未通过任一门禁时必须明确报告失败并停止，不得继续扩展范围。
