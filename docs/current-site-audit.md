# 当前生产站只读审计记录

**目标：** `https://japanairporttransfer.com/`  
**审计时间：** 2026-07-11（GMT+9）  
**审计方式：** 公开页面只读访问；尚未登录后台；未进行任何生产修改。

## 一、公开首页现状

公开首页可通过 HTTPS 访问，页面标题为 `japanairporttransfer.com`。目前展示 WordPress 初始文章提示 `Welcome to WordPress. This is your first post. Edit or delete it, then start writing!`，说明公开内容仍处于初始化状态。

| 检查项 | 当前观察 | 风险/处理 |
|---|---|---|
| 页面语言 | 同时出现英文初始化文字、中文表单标签及占位翻译键 | 不满足全日语要求；需在测试环境重建后统一日语 |
| 导航 | 可见 `trans-menu` 占位键，没有有效业务导航 | 当前无法引导用户了解服务或预约 |
| 联系信息 | `trans-contact_email`、`trans-contact_phone` 等占位键 | 未配置真实且经确认的联系方式 |
| 社交链接 | Facebook、Instagram、TikTok、WhatsApp 指向占位变量 | 可能产生无效外链；上线前必须移除或配置经确认链接 |
| Newsletter | 显示中文“电子邮件”“订阅” | 与 100% 日语要求冲突；首期是否保留订阅需评估 |
| 页脚 | `trans-current-year`、`trans-all-rights-reserved` 未被替换 | 主题翻译/模板配置不完整 |
| 核心内容 | 未见服务、地点、流程、费用、FAQ、会社、法务或预约入口 | 不满足任务信息架构 |
| 生产成熟度 | 初始文章与主题占位内容直接公开 | 应尽快在测试环境完成替换；不在生产直接试错 |

## 二、已确认的公开风险

当前生产站不是可接受的举牌迎送业务网站，存在多语言混用、主题变量未解析、无业务导航、无信任信息、无预约入口和占位社交链接等问题。后续应先在受保护测试环境完成主题、插件、内容与预约闭环，再采用可回滚方式发布生产。

## 三、待后台审计项目

下一步需以只读方式核对 WordPress/PHP/数据库版本、活动主题、已安装插件、固定链接、用户角色、站点健康、缓存、安全、邮件、现有页面/文章/媒体和搜索引擎可见性。登录审计不得保存或输出用户凭据，不得修改设置或内容。

## 四、证据文件

本次公开访问的页面截图由浏览环境临时保存，仅用于内部审计，不进入 Git 公开历史，也不包含客户个人资料。页面文本提取文件同样作为临时只读证据，不作为生产内容来源。

## 五、后台初步只读信息

已使用中村先生授权的账户成功登录后台，仅查看页面，未创建用户、未修改设置、未安装/更新插件、未编辑内容。

| 检查项 | 当前观察 | 说明 |
|---|---|---|
| WordPress 版本 | 7.0.1 | 后台页脚显示版本号 |
| 当前账户 | 已授权的管理员执行账号（用户名不入库） | 可见“添加用户”和管理员相关菜单，推定具备较高权限；仍需通过角色页面确认 |
| 可用更新 | 1 项 | 尚未打开更新详情，未执行更新 |
| 主机集成 | Hostinger 菜单与工具可见 | 可能由 Hostinger 托管；需继续核对测试环境、备份和主机工具能力 |
| 缓存 | LiteSpeed Cache 菜单可见 | 已安装并可能启用；后续开发需验证缓存与预约 Nonce/动态页兼容性 |
| 后台语言 | 主要为简体中文，站点默认语言选项显示 English (United States)/简体中文 | 与最终前台全日语不同；需在测试环境规划站点语言和管理员个人语言 |
| 站点编辑 | 可见完整站点编辑器入口 | 当前主题可能为区块主题或兼容站点编辑器，需在主题页确认 |
| 隐私工具 | 可见导出/抹除个人数据 | 后续预约插件应集成或至少兼容 WordPress 隐私流程 |
| 当前操作 | 仅登录并查看“添加用户”页面 | 未提交任何表单，生产数据无变更 |

## 六、插件清单（只读）

后台显示共 **5 个常规插件，5 个均已启用**；另有 2 个强制使用插件和 1 个强化扩展（名称尚待核对）。本次没有执行更新、启停或删除。

| 插件 | 当前版本 | 状态 | 审计备注 |
|---|---:|---|---|
| Hostinger AI | 3.0.48 | 已启用 | 首期自研主题/插件不应依赖该插件 |
| Hostinger Reach | 1.5.8 | 已启用，有 1.5.9 更新 | 当前唯一可见待更新项；备份和测试前不在生产更新 |
| Hostinger Tools | 3.0.72 | 已启用 | 需用于核对主机工具、备份或测试环境能力 |
| Hostinger 快速使用流程 | 2.1.31 | 已启用 | 建站完成后可评估是否仍需要，但未经批准不在生产停用 |
| LiteSpeed Cache | 7.8.1 | 已启用 | 预约页、Nonce、REST/AJAX、成功页和后台订单须配置缓存排除并测试 |

**初步风险：** 当前站点全部常规插件均为主机/缓存类，没有现成表单、SEO、安全或 SMTP 插件。P0 预约业务应继续采用专属 `jat-reservation` 插件；邮件送达、SEO 与安全是否通过轻量插件或代码实现，应在测试环境兼容性验证后决定，避免无必要堆叠插件。

## 七、主题清单（只读）

后台共显示 4 个已安装主题，本次未切换、更新、删除或进入编辑器修改。

| 主题 | 版本 | 状态 | 处理原则 |
|---|---:|---|---|
| Hostinger AI 主题 | 2.0.29 | 当前启用 | 现有占位内容来源；待测试环境中以专属 `jat-meet-theme` 替换，不在生产直接改源码 |
| 二〇二三 | 1.6 | 未启用 | WordPress 官方备用主题之一 |
| 二〇二五 | 1.5 | 未启用 | WordPress 官方备用主题之一 |
| 二〇二四 | 版本待核对 | 未启用 | WordPress 官方备用主题之一 |

当前启用的是区块主题，后台提供完整站点编辑器。专属主题可继续采用区块主题架构并通过 `theme.json`、模板、模板部件与受控区块实现，但业务预约逻辑必须保持在独立插件中，避免主题切换导致订单能力丢失。

## 八、常规设置（只读）

本次只查看常规设置，没有点击保存。

| 设置 | 当前值 | P0 处理建议 |
|---|---|---|
| 站点标题 | `japanairporttransfer.com` | 测试环境改为经确认的日文品牌标题 |
| 副标题 | 空 | 上线前补充简洁日文价值主张 |
| WordPress 地址 | `https://japanairporttransfer.com` | 保持 HTTPS；迁移/测试环境须使用独立 URL |
| 站点地址 | `https://japanairporttransfer.com` | 同上 |
| 管理员邮箱 | 已配置为个人 Gmail 地址 | 属管理信息，不在公开交付文档展开；正式客服/系统告警地址仍需中村先生确认 |
| 任何人可注册 | 未勾选 | 符合首期无客户账户设计，建议保持关闭 |
| 站点语言 | 简体中文 | 测试环境应切换为日本語，并验证主题、插件、浏览器校验、邮件及系统提示无中文残留 |
| 时区 | UTC+0 | 应改为 `Asia/Tokyo`/东京，避免订单、邮件、审计和预约时间出现 9 小时偏差 |
| 日期/时间格式 | 当前为中文环境默认格式 | 日语环境下统一显示并以服务器端东京时区计算 |
| 站点图标 | 页面显示选择入口，未确认有效图标 | 等授权品牌素材后配置；不得临时上传未经确认 Logo |

**高优先级风险：** 当前站点时区为 UTC+0，而业务发生在日本。预约插件不得依赖 PHP/服务器默认时区，应使用 WordPress 时区 API；测试环境切换东京时区后必须覆盖跨日、夏令时无关性和邮件时间显示测试。

## 九、阅读设置（只读）

当前页面没有显示“首页显示”为静态页面的选项，说明站点尚未创建可供选择的页面，当前公开首页直接展示最新文章。搜索引擎可见性复选框未勾选，即生产站目前允许被搜索引擎索引。本次未保存设置。

| 检查项 | 当前值 | 处理原则 |
|---|---|---|
| 首页展示 | 最新文章/无静态首页选项 | 测试环境创建页面树后设置专属静态首页 |
| 博客每页 | 10 篇 | 可保留，通知列表另按设计分页 |
| Feed | 最近 10 项，全文/摘要当前需进一步通过值确认 | 上线前设为摘要以降低全文复制风险 |
| 搜索引擎可见性 | 未勾选“不索引”，即允许索引 | 生产上线后可索引；测试环境必须开启 noindex，并配合认证/robots，不能只依赖该复选框 |

**SEO 风险：** 当前初始化内容已公开且可被索引。发布前应检查搜索引擎缓存、XML sitemap、canonical 与初始文章 URL，必要时删除/重定向，避免低质量占位页面持续收录。

## 十、固定链接（只读）

当前固定链接结构为 `/%postname%/`，分类目录与标签前缀为空。本次未保存任何设置。

该结构可直接支持批准计划中的简洁罗马字路径，例如 `/service/airport-meet/`、`/location/haneda/`、`/reservation/`。正式创建页面前应冻结 slug 规范，并为现有初始文章 URL 制定删除或 301 重定向策略，避免上线后频繁变更路径。

## 十一、用户与权限（只读）

当前站点共有 2 个用户，二者均为管理员；其中本次授权执行账号没有文章，另一管理员账号拥有 1 篇初始化文章。本次未新增、编辑、降权、删除用户，也未发送密码重置邮件。

| 风险 | 级别 | 建议 |
|---|---|---|
| 两个日常账号均拥有管理员权限 | 中 | 上线前确认账号归属；日常内容编辑建议使用编辑角色，管理员仅用于维护 |
| 当前授权密码已在任务消息中明文传递 | 高 | 完成开发与交付后由中村先生立即轮换，并启用主机/WordPress 可用的双重验证或登录保护 |
| 管理邮箱为个人邮箱 | 中 | 需要确认正式系统通知邮箱、客服收件地址和隐私请求地址，三者可分离 |
| 无专用测试/审计账号 | 中 | 测试环境应创建独立、可撤销账号；生产部署完成后撤销临时访问权限 |

交付仓库、截图、日志、`truth.md` 与文档中禁止记录任何明文密码、登录 Cookie、nonce 或恢复链接。

## 十二、现有页面（只读）

当前仅有 1 个页面：`Privacy Policy`，slug 为 `privacy-policy`，状态为草稿，并已被指定为隐私政策页。没有已发布的静态页面，本次未编辑或删除。

后续应保留该草稿作为迁移参考，在测试环境依据批准资料重写为完整日文隐私政策，并在发布前核对公司主体、数据处理目的、保存期限、第三方服务、跨境处理、联系方式与个人数据请求流程。未经法务/业务确认的条款不得标记为最终版。

## 十三、现有文章（只读）

当前仅有 1 篇已发布文章：`Hello world!`，slug 为 `hello-world`，分类为 `Uncategorized`，也是当前公开首页显示的占位内容。本次未编辑、下架或删除。

上线切换时应将该占位文章移入回收站，并将 `/hello-world/` 进行 301 重定向至正式首页或合适的通知页；同时移除未使用的默认分类展示。此项必须在备份与测试环境验证后执行。

## 十四、媒体库（只读）

当前媒体库为空，没有 Logo、品牌图标、机场现场照片、人员照片或其他已授权素材。本次未上传任何文件。

因此主题开发阶段只能使用代码生成的基础图形、占位框和明确可商用的自有/授权素材。两个参考网站及 PDF 中的图片仅用于理解业务与布局，不得直接复制。正式视觉上线依赖中村先生提供品牌 Logo、公司/现场照片及相应使用授权；若未提供，应采用无侵权风险的文字品牌与中性图形方案。

## 十五、站点健康（只读）

当前 WordPress 站点健康报告显示 1 个关键问题与 2 个推荐改进。本次未展开修复、更新、删除主题或运行计划任务。

| 类型 | 当前提示 | 处理顺序 |
|---|---|---|
| 关键问题 | 有插件等待更新 | 完整备份并在测试环境做兼容性测试后更新，禁止在未备份的生产站直接更新 |
| 推荐改进 | 应移除未启用主题 | 至少保留一个 WordPress 官方默认主题作为故障回退，其余确认无依赖后再删除；不能机械执行全部删除 |
| 推荐改进 | 一个计划事件被延迟 | 核对 WP-Cron/Action Scheduler、主机 cron 与具体 hook；预约邮件和清理任务不得依赖未验证的伪 cron |

延迟计划事件对预约确认邮件、状态通知和数据清理具有直接风险。正式插件应支持幂等重试、可审计日志与管理员告警，并在测试环境模拟 WP-Cron 延迟场景。

## 十六、服务器运行环境（只读）

站点运行于 Linux + LiteSpeed，PHP 版本为 8.3.30（64 位），PHP SAPI 为 LiteSpeed。主要限制为：`max_input_vars=5000`、执行时间 360 秒、内存限制 2048M、最大上传与 POST 均为 2048M，cURL 8.14.1 / OpenSSL 3.5.1。对象缓存可用且已启用，当前显示 51MB/256MB；服务器支持固定链接，存在自定义 `.htaccess` 规则，`robots.txt` 为 WordPress 生成的动态文件。

这些资源足以支持本期主题与预约插件，但开发必须以 PHP 8.3 严格兼容为基线，避免动态属性、弃用 API 和隐式类型问题。预约表单不得依赖超大 POST；所有字段应显式白名单、分层验证并控制提交体积。LiteSpeed 页面缓存、对象缓存与预约接口必须做排除和失效策略，避免缓存用户输入、nonce、确认页面或订单状态。

## 十七、数据库运行环境（只读）

数据库扩展为 `mysqli`，服务器为 MariaDB 11.8.8，客户端为 mysqlnd 8.3.30，字符集为 `utf8mb4`，排序规则为 `utf8mb4_unicode_520_ci`，表前缀为标准 `wp_`。具体数据库名称、数据库用户和主机信息属于敏感运行配置，未写入项目文档。

预约插件的数据表迁移应使用 `dbDelta()` 或受控版本迁移，并显式采用站点字符集/排序规则；索引长度需兼容 utf8mb4。订单号、幂等键、状态、创建时间、服务时间与软删除/匿名化字段必须建立必要索引。任何迁移先在克隆测试库验证，生产执行前生成数据库导出并记录插件 schema 版本。

## 十八、Hostinger WordPress 工具（只读）

WordPress 内的 Hostinger 工具页可见 WordPress/PHP 版本、LLMs.txt/网络代理选项与维护模式，但当前已登录权限范围内没有显示完整站点备份、数据库快照、文件管理器或一键测试环境入口。维护模式可阻止公众访问并提供旁路链接，但本次未启用。

因此当前存在**完整回滚点缺口**：仅靠 WordPress 内容导出不足以恢复插件、主题、配置和数据库。下一步必须先建立文件与数据库双备份并验证可下载/可读取，之后才能安装专属代码、更新插件或切换主题。若后台备份插件无法在不影响生产的前提下完成，则需请求 Hostinger hPanel 权限或主机方生成快照；未建立可验证备份前，不执行生产结构变更。
