# 域名迁移变更记录：meetandgreet.jp

**日期：** 2026-07-15（GMT+9）

**执行角色：** ②全栈工程师AI（生产部署）

**汇报对象：** PM-AI（通过中村先生转达）

**基线提交：** `4758bc3`

## 一、任务范围

本次仅处理主域名由 `japanairporttransfer.com` 变更为 `meetandgreet.jp` 的仓库基线、自动化验收和生产只读核查。未修改生产主题、插件、数据库、页面内容、DNS、证书或主机设置。

| 范围 | 完成结果 |
|---|---|
| 公开元信息 | 主题公开地址更新为 `https://meetandgreet.jp/` |
| 项目入口 | README、`truth.md`、`plan.md`、现站审计和 QA 报告更新为新域名及真实迁移状态 |
| 运行时代码 | SEO canonical、REST 同源校验、邮件链接均确认继续动态读取 WordPress URL，无需新增硬编码 |
| 本地测试 | 继续使用 `127.0.0.1:8080`，禁止测试直接命中生产 |
| 域名验收 | 新增 `tests/domain-migration-readonly.sh`，只执行 DNS、HTTPS、证书、canonical、WordPress URL 与旧域 301 检查，不创建生产订单 |
| 审计证据 | 新增 `docs/domain-migration-2026-07-15.md`，记录核查结果和基础设施阻断项 |

## 二、验证结果

| 检查 | 结果 |
|---|---|
| PHP / JavaScript / Shell 语法 | 通过 |
| 阶段四预约集成 | 23/23 通过 |
| REST 安全与恢复 | 11/11 通过 |
| 阶段五运营后台 | 31/31 通过 |
| 阶段六业务边界 | 138/138 通过 |
| 全站质量抓取 | 22 个公开页面通过；6 个业务/法务页面按既有门禁保持草稿 |
| 差异完整性 | `git diff --check` 通过 |
| 凭据扫描 | 通过 |
| 运行时旧域硬编码 | `wp-content/` 与 `bin/` 无旧域引用 |
| 域名迁移只读门禁 | 5 项通过、2 项失败 |

域名门禁通过项为新根域 HTTPS、根域证书、首页 canonical、WordPress URL 与运行时代码扫描。失败项为 `www.meetandgreet.jp` 的 TLS 证书未覆盖 `www`，以及旧域名当前无法解析，不能验证按原路径跳转到新域的 301。

## 三、仍未解除的生产门禁

1. 在 Hostinger 中为 `www.meetandgreet.jp` 配置有效证书，并将 `www` 规范化 301 到根域。
2. 恢复旧域名 `japanairporttransfer.com` 的 DNS/主机接入，并按原路径永久 301 到 `https://meetandgreet.jp`；旧域证书也必须有效，避免 HTTPS 请求在跳转前失败。
3. 建立并验证生产文件、数据库和主机配置的完整备份与可回滚点。
4. 取得 PM-AI 对本次提交哈希的独立验收，以及中村先生转达的明确生产上线授权。
5. 既有公司、价格、地点、法务、邮件和授权素材门禁仍未解除，不得因域名变更而绕过。

## 四、结论

仓库与本地测试基线已完成新域名更新，生产根域也已由外部变更生效；但完整 SEO 域名迁移尚未闭环。当前状态为“可提交 PM-AI 审查、不可声称迁移全部完成、不可继续生产结构部署”。
