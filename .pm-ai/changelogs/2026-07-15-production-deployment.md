# 生产部署与独立验收变更记录

**日期：** 2026-07-15（GMT+9）

**执行角色：** ②全栈工程师AI（生产部署）

**汇报对象：** 中村先生（按 2026-07-15 最新授权独立执行与验收）

**生产站：** `https://meetandgreet.jp/`

## 一、任务范围

本次在中村先生明确授权独立完成全部任务后，先建立生产可恢复点，再以最小文件级方式部署固定页头修复和招聘页草稿。发现生产主题版本高于仓库基线后，停止整包覆盖，保留生产专有改动并将生产主题回同步到仓库。

| 范围 | 完成结果 |
|---|---|
| 部署前备份 | 使用 UpdraftPlus 创建完整恢复点，包含数据库、插件、主题、上传、MU 插件及其他 `wp-content` 内容 |
| 固定页头 | 已部署并在桌面、滚动及 390px 移动布局中通过回归 |
| 招聘页 | 创建为草稿，页面 ID `94`，slug `recruit`，未公开 |
| 生产主题 | 缓存版本最终提升至 `0.2.5` |
| 移动溢出 | 为首页举牌输入框增加局部 `box-sizing: border-box`，生产 390px 布局无整页横向溢出 |
| 仓库同步 | 以生产 `0.2.5` 为基线回同步 `style.css`，避免后续部署降级覆盖生产专有样式 |
| 临时代码 | 一次性部署插件执行成功后已停用并删除 |
| 备份能力 | UpdraftPlus 保持启用，部署前完整恢复点可见且可恢复 |

## 二、生产验收

| 检查 | 结果 |
|---|---|
| 首页与固定页头 | 真实访客会话加载 `0.2.5`；页头初始与滚动后均保持固定 |
| 桌面布局 | 通过，无横向溢出 |
| 390px 移动布局 | 菜单断点、固定页头、举牌输入框盒模型及零整页溢出通过 |
| 招聘草稿预览 | 内容区块、CTA、桌面与移动布局通过；保持草稿 |
| 外部 HTTP | 首页 200、预约页 200、联系页 200、招聘公开路径 404 |
| WordPress 站点健康 | 严重问题 0；三项既有维护建议未擅自修改 |
| PHP 语法 | 主题及业务插件全部通过 `php -l` |
| 差异完整性 | `git diff --check` 通过 |
| 敏感信息 | 授权凭据未写入 Git、文档或构建产物 |
| 隔离环境完整回归 | 阶段四 23/23、REST API 11/11、阶段五 31/31、业务边界 138/138、固定页头与招聘页 E2E、22 页全站质量门禁全部通过；招聘测试页已恢复草稿 |
| 域名只读门禁 | 主域 HTTPS、canonical、WordPress URL、根域证书和旧域硬编码检查通过；`www` 证书及旧域按路径 301 为本次部署前已知的独立 SEO 迁移事项 |

## 三、证据与回滚

完整执行记录见 `docs/production-deployment-2026-07-15.md`，最终 HTTP 冒烟测试见 `docs/qa-assets/final-production-http-qa.txt`。如需回滚，可在 WordPress 后台的 UpdraftPlus 恢复部署前完整备份；生产操作前的一次性部署助手也建立了文件快照并在执行异常时具备自动回滚逻辑，任务完成后其代码已从生产删除。

## 四、剩余业务决策

招聘页继续保持草稿，正式公开前仍需确认薪资、奖金、排班、福利、联系方式与个人信息处理口径。站点健康中的搜索引擎索引建议属于独立 SEO 决策，本次未修改。域名迁移仍需补发覆盖 `www.meetandgreet.jp` 的证书，并恢复旧域名按原路径到新域的 301；两项均为本次固定页头与招聘草稿部署前已存在的问题，不影响本轮功能验收结论。
